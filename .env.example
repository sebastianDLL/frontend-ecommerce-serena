# URL base de la API (backend NestJS), incluyendo el prefijo de versión.
PUBLIC_API_URL=http://localhost:3000/api/v1

# Muestra productos y pedidos de ejemplo cuando la API no está disponible.
# En `astro dev` el modo demo se activa automáticamente.
PUBLIC_DEMO_MODE=false
